import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
    paddingVertical: 10,
  },
  flatlistContainer: {
    gap: 50,
    justifyContent: 'center',
    alignItems: 'center',
  },
});
